<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sc.android.chestwork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".db.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCloseCameraActivity" />
        <activity android:name=".a8266.ControlActivity" />
        <activity
            android:name=".ShowAddCloseActivity"
            android:label="@string/title_activity_show_add_closectivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".ConnectSocketActivity" />
        <activity android:name=".Query_pic_Activity" />
        <activity android:name=".ShowRecyclerViewDetailsActivity" />
        <activity android:name=".TakeActivity" />
        <activity
            android:name=".DeleteActivity"
            android:label="@string/title_activity_delete"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Query_RecyclerView_Activity"></activity>
    </application>

</manifest>